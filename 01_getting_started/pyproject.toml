[project]
name = "getting-started"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "snowflake",
    "snowflake-snowpark-python>=1.5.0,<2.0.0",
    "rich",
    "jupyter",
    "ipykernel>=7.2.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
